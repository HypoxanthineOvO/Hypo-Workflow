# s69 Audit Regression Canonical Examples

- [ ] Focused red test requires canonical examples under `core/test/fixtures/audit-regression-canonical-examples/`.
- [ ] Audit mid-flight rejection example cites structured rejection artifact and deterministic rework next step.
- [ ] Audit-approved blocked example shows `implement` proposal plus `audit` approval and rejects implement self-approval.
- [ ] Cross-role `test`/`implement` example rejects shared worker identity or ownership/scope violation.
- [ ] Missing audit planning question example blocks P1 from entering P2 when the P1 audit question group is absent.
- [ ] Canonical examples cite real focused test commands and s64-s68 scenario `run.sh` commands.
