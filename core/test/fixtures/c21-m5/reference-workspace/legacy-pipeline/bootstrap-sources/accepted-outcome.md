# Accepted Outcome

C21-M1 through C21-M4 established certified transaction, Record, Recovery, Init, and read-only legacy
inspection boundaries. Bootstrap must reuse those boundaries instead of creating parallel authority.
