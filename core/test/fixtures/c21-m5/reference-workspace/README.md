# Redacted C21 Bootstrap Fixture

This synthetic repository models a legacy workspace immediately before the C21-M5 cutover.
Its legacy state intentionally stops at M4; the new Runtime, Continuation, and Recovery Pack
must carry M5 and M6 after activation. It contains no copied project history, conversation,
tool output, credentials, machine-local paths, or unreviewed confidential material.
