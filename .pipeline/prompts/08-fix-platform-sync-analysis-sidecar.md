# M8 — P1 平台版本同步

## 目标
同步 OpenCode/Claude Code 生成物到 v12.7.0，创建 analysis sidecar。

## F103: 平台生成物版本同步
1. 更新 `core/src/artifacts/opencode.js` 中的 `HW_VERSION` 常量从 12.5.2 到 12.7.0
2. 更新 `core/src/artifacts/claude.js` 同样
3. 运行 `hypo-workflow sync --platform opencode` 重新生成
4. 验证 `.opencode/package.json` 和 `.opencode/plugins/hypo-workflow.ts` 版本号变为 12.7.0

## F203: 创建 analysis sidecar
**文件**: `.opencode/hypo-workflow.json.analysis`

创建最小 analysis sidecar，内容参考 AGENTS.md 引用：
```json
{
  "execution": {
    "preset": "analysis",
    "steps": {"preset": "analysis"},
    "analysis": {
      "interaction_mode": "hybrid",
      "allow_code_changes": false,
      "require_confirmation_for_restart": true,
      "require_confirmation_for_system_install": true
    }
  },
  "boundaries": {
    "network": "ask",
    "remote_resources": "ask",
    "destructive": "ask",
    "external_side_effects": "ask"
  }
}
```

## F204: commandMap 双源注释
在 `.opencode/plugins/hypo-workflow.ts` 的 commandMap 顶部添加注释：
"Source of truth for command map. `.opencode/hypo-workflow.json` is a derived copy generated by sync."

## 验收
- sync 后生成物版本均为 12.7.0
- `.opencode/hypo-workflow.json.analysis` 存在
- commandMap 注释到位
