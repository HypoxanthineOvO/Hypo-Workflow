# Release Report: v12.8.1

- 版本：12.8.1
- 日期：2026-05-17
- 版本策略：从 12.8.0 按用户要求 `+0.0.1`，作为 patch bump 处理为 12.8.1。
- 基线：`v12.8.0` 到 GitHub PR #8 merge commit `930b5620c4d9d0f4ccd862f434a9497e664c285e`。

## 变更摘要

- 合入 PR #8：Cursor adapter 现在生成每命令 flat Skill 和 slash command 入口。
- 新增 `.cursor/hypo-workflow/` compact resource bundle，用于 Cursor 目标项目的共享 references/assets/scripts/adapters。
- 修复 Cursor `/hw-setup` 指向未镜像 `references/config-spec.md` 的自洽性问题，改为 self-contained authority + external/non-local fallback。
- 修复发布回归中发现的 PR lifecycle log schema 漏项：`pr` family 与 `review_ready`、`remote_review_submitted`、`rereview_passed`、`merged` 状态现在被 validator 和 `references/log-spec.md` 支持。
- 保留 `.pipeline/pr/PR-20260517-001/` 本地 PR 审阅归档，记录 request changes、复审、approve 和 merge。

## 版本与文档

- 同步版本源到 12.8.1：`.claude-plugin/plugin.json`、`.codex-plugin/plugin.json`、`.opencode/package.json`、`cli/package.json`、`cli/package-lock.json`、`core/package.json`、`.claude-plugin/marketplace.json`、`core/src/config/index.js`、OpenCode/Claude generator `HW_VERSION`。
- 新增 `CHANGELOG.md` v12.8.1 条目。
- 新增中英文 release notes：`docs/release/v12.8.1.md`、`docs/en/release/v12.8.1.md`。
- README / README.en 和 docs governance registry 指向 v12.8.1。

## 验证结果

- `node --test core/test/log-evidence.test.js`: 5/5 passing，确认 PR lifecycle log family/status schema 修复。
- `node --test core/test/platform-adapters.test.js core/test/profile-platform.test.js`: 13/13 passing。
- `node --test core/test/docs-governance.test.js core/test/readme-update.test.js core/test/skill-spec.test.js core/test/sync-standardization.test.js core/test/commands-rules-artifacts.test.js core/test/platform-adapters.test.js core/test/profile-platform.test.js`: 42/42 passing。
- `node --test core/test/*.test.js`: 516/516 passing。
- `uv run python tests/run_regression.py`: 68/68 passing。
- `checkDocs('.')`、`checkDocsLanguage('.')`、`checkNarrativeDocsForRelease('.')`、`checkReadmeFreshness('README.md')`: passing。
- `node cli/bin/hypo-workflow sync --platform opencode --project . --check-only`: derived=fresh。
- `git diff --check`: passing。

## 发布步骤

1. 更新版本源、README、CHANGELOG、release notes 和 docs registry。
2. 运行验证计划。
3. 提交 `release: v12.8.1`。
4. 打 tag `v12.8.1`。
5. 推送 GitHub 和 GitLab main/tag。
6. 创建 GitHub release。
