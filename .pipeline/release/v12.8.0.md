# Release Report: v12.8.0

- 时间：2026-05-16T13:09:34+08:00
- 版本：12.8.0
- 版本策略：从 12.7.0 按用户要求 `+0.1`，作为 minor bump 处理为 12.8.0。
- 范围：Sync + Docs + Release。用户已在本地准备完成后确认 release gate；git commit、tag、push 和远程 release publish 由本报告写入后的发布步骤执行。

## 改动摘要

- 同步版本源到 12.8.0：`.claude-plugin/plugin.json`、`.codex-plugin/plugin.json`、`.opencode/package.json`、`cli/package.json`、`cli/package-lock.json`、`core/package.json`、`.claude-plugin/marketplace.json`、`core/src/config/index.js`、OpenCode/Claude generator `HW_VERSION`。
- 新增 `CHANGELOG.md` v12.8.0 条目。
- 新增中英文 release notes：`docs/release/v12.8.0.md`、`docs/en/release/v12.8.0.md`。
- README / README.en 更新版本 badge 和 release notes 入口。
- `core/src/docs/index.js` 与 `core/test/docs-governance.test.js` 更新到当前 release note source。
- 修正历史 `v12.6.0` 和 `v12.5.2` release note 中误指向 v12.7.0 的标题/互链。
- 运行 OpenCode sync repair，刷新 OpenCode adapter、metadata、plugin 和 compact/derived health。

## 技术思路

先把 canonical version sources 更新为 12.8.0，再运行 `hypo-workflow sync --repair`，确保生成物从新版本源派生，而不是手工分别改 generated artifacts。README 和 release notes 属于 release-flow 文档，按当前 narrative 边界手动更新，不触发完整 README 重写。

## 验证

- `node cli/bin/hypo-workflow sync --platform opencode --project . --repair`: `derived=fresh`。
- `node cli/bin/hypo-workflow sync --platform opencode --project . --check-only`: `derived=fresh`。
- `checkDocs('.')`: ok。
- `checkNarrativeDocsForRelease('.')`: ok。
- `checkDocsLanguage('.')`: ok，17 files checked。
- Focused docs/sync/OpenCode tests：34/34 passed。
- `uv run python tests/run_regression.py`: 68/68 passed。

## 发布 Gate

本地 release 准备已完成，用户已确认继续发布。按 release safety rules，commit、tag、push 和远程 release publish 作为显式 gate 后续步骤执行，并在最终回复中报告结果。
