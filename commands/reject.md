---
description: Hypo-Workflow mapping for /hw:reject
hypo_workflow_managed: true
---

# /hw:reject

Canonical command: `/hw:reject`
Skill: `skills/reject/SKILL.md`

加载 `skills/reject/SKILL.md`，按其中的 `/hw:reject` 语义处理用户参数。
这是 Claude Code slash-command 映射，不是独立 runner。Claude Code 执行实际工作，Hypo-Workflow 语义文件保存计划、进度、讨论和历史。
