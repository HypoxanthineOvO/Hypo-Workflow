---
description: Hypo-Workflow mapping for /hw:goal
hypo_workflow_managed: true
---

# /hw:goal

Canonical command: `/hw:goal`
Skill: `skills/goal/SKILL.md`

加载 `skills/goal/SKILL.md`，按其中的 `/hw:goal` 语义处理用户参数。
这是 Claude Code slash-command 映射，不是独立 runner。Claude Code 执行实际工作，Hypo-Workflow 语义文件保存计划、进度、讨论和历史。
